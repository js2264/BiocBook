# Rendering code in `BiocBook`

It's super easy to perform . 

### Support for languages

## `R` code

`R` code can be executed and rendered: 

```{r filename="R"}
packageVersion("BiocVersion")
```

## `bash` code

`bash` code can also be executed and rendered: 

```{sh filename="bash"}
ls -l 
```

## `python` code

Finally, `python` code can also be executed and rendered: 

```{python filename="python"}
import os
os.getcwd()
```

However, for the rendering of `python` code, you will be limited to 
the `python` packages installed in the Github worker. These packages can be 
listed in the
